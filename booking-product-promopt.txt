<role>
You are an expert creative frontend engineer specializing in optimized, production-ready, accessible, and responsive HTML, CSS, and vanilla JavaScript component architecture.
</role>

<task>
Generate a premium, high-end Dedicated Product Page and two global interactive overlay systems (Booking Modal and Direct Checkout Modal) for a luxury professional barber. The interfaces must be fully designed for Persian (FA) localization using strict RTL structural layouts.
</task>

<context>
<current_basic_css>
:root {
    --orange: #fc4103;
    --orange-transparent: #fc410362;
    --orange-less-transparent: #fc4103b9;
    --orange-light: #ff845c;
    --dark-blue: #0F172A;
    --dark-blue-transparent: #0F172A59;
    --black: #0A0A0A;
    --white: #cfcfcf;
    --bg-main: #0a0a0aab;
    --gray: #BFC1C2;
    --gold: #C69B3C;
    --overlay: rgba(0, 0, 0, 0.75);
    --container-width: 1400px;
    --transition-default: 0.8s cubic-bezier(0.25, 1, 0.5, 1);
    --radius-sm: 0.3rem;
    --radius-md: 0.8rem;
    --radius-lg: 1.5rem;

    --bg-main: var(--black);
    --text-main: var(--white);
    --text-main-reverse: var(--black);
    --text-muted: var(--gray);
    --accent: var(--orange);
    --nav-bg: rgba(10, 10, 10, 0.699);
    --border-color: rgba(255, 255, 255, 0.05);

    --font-display: 'Fraunces', Georgia, serif;
    --font-body: 'Manrope', -apple-system, sans-serif;
    --font-mono: 'Space Mono', monospace;
}

[data-theme="light"] {
    --bg-main: #F8F9FA;
    --text-main: #111111;
    --text-muted: #666666;
    --text-main-reverse: var(--white);
    --accent: var(--dark-blue);
    --nav-bg: rgba(248, 249, 250, 0.699);
    --border-color: rgba(0, 0, 0, 0.08);
}

body {
    margin: 0;
    background: var(--bg-main);
    color: var(--text-main);
    font-family: var(--font-body);
    font-weight: 400;
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
    transition: background var(--transition-default), color var(--transition-default);
    overflow-x: hidden;
}
</current_basic_css> 

<preferred_theme_rules>
- Theme Continuity: Maintain the dark aesthetic. Deep blacks, muted whites, gold highlights, and vibrant premium orange accents.
- Animation Dynamics: Deliberate, smooth transitions (0.5s - 1.8s) utilizing cubic-bezier values.
- UI Element Composition: Blur filters (`backdrop-filter: blur()`) on overlay backgrounds. 
- Strict Product Geometry: Product cards MUST have `border-radius: 0`. They should feel sharp, editorial, and raw.
</preferred_theme_rules>

<page_architecture_and_layouts>
1. Dedicated Product Page (`products.html`):
   - A minimalist, editorial heading area (in Persian).
   - A CSS Grid showcasing the products. 
   - Product Card Design: ZERO border-radius. High-resolution image base, rich dark gradient overlay rising from the bottom. Placed over the gradient: Product Name, product description, Price (in Tomans), and a prominent "Buy" (`خرید`) button.

2. Global Booking Modal (Pop-up Overlay):
   - Mobile-First Layout: Stacked. Top half contains Service Selection; Bottom half contains Booking Flow/Auth.
   - Desktop Layout: Split-screen Grid. Left side contains Service Selection; Right side contains Booking Flow/Auth.
   - Selection Area (Two Categories):
     * Category 1 (Normal Services): List items showing Name, Time/Duration, and Price Range.
     * Category 2 (Grooming Packages): List items showing Package Name (A or B), Price Range, and a "More Info" link strictly routing to the grooming page.
   - JavaScript Pre-selection logic: The JS must allow a specific service to be pre-selected when the modal opens via `data-service-id` attributes on external trigger buttons.
   - The JS engine must search for any element with a `data-open-booking` attribute, extract its `data-service-id`, open the modal, and auto-focus/select that target item.
   - Flow & Auth Area (Right/Bottom):
     * State A (Unauthenticated): Shows a premium "sign in to continue" message with an inline Login/Signup form(it is already built, don't code it.) view (so the user doesn't lose their selected service).
     * State B (Authenticated): Shows Date/Time picker, Bypass Code input, and the final 500,000 Tomans deposit summary with the "Proceed to Gateway" CTA.
     - The Date Picker input element must programmatically enforce a boundary limit of exactly 180 days (6 months) from the current date.
   - Provide a simple global JavaScript constant `const IS_AUTHENTICATED = false;` at the top of the script so I can toggle between State A and State B manually during staging.

3. Global Product Checkout Modal (Pop-up Overlay):
   - Triggered by the "Buy" button on product cards.
   - Flow:
     * State A (Unauthenticated): Prompts an inline Sign In form(already designed, skip, only leave space for pasting the code).
     * State B (Authenticated): Displays Product Summary + an auto-filled Shipping Form (Name, Phone, Address) simulating database pre-fill, followed by "Proceed to Payment".
</page_architecture_and_layouts>

<exclude_and_skip>
    - top navigation bar
    - side navigation panel
    - global footer elements
    - sign in / sign up(instead, use empty div to paste the current code for sign in, and sign up, and the js to change between them.)
    - root css variables (assume already loaded in memory)
    - general css styling resets (assume already loaded in memory)
</exclude_and_skip>
</context>

<rules>
1. Responsive Priority: Design mobile-first. The Booking Modal layout shift (Top/Bottom to Left/Right) is critical.
2. Code Standards: Strict BEM (Block-Element-Modifier) naming conventions.
3. Documentation: Limit comments to media query block indicators (e.g., `/* Media Query - Tablet */`).
4. Bi-Directional & RTL Layout: Use CSS Logical Properties (`margin-inline`, `padding-inline`, `inset-inline`). The HTML root must use `dir="rtl"`. All static UI copy must be premium Persian (Farsi).
5. Output semantic HTML, CSS, and modular Vanilla JS for handling the modal states and pre-selection logic.
</rules>

<output_format>
Provide the code inside 4 distinct, cleanly divided files(seperate what codes are for booking and what are for shoping by comments):
- products.html (The separate shop page containing the direct checkout overlay HTML)
- booking-overlay.html (The standalone booking modal overlay HTML component)
- shop-booking.css (The combined stylesheets for the shop page and both modals)
- shop-booking.js (The combined JavaScript for state toggling, pre-selection, and form submissions)
</output_format>



Be extremely brief. Omit all pleasantries, explanations, and introductory fluff. Provide only the direct code or brief, scannable steps.



[CONTEXT] 
Working on an RTL modal component.

[CLEAN SNIPPET]
// Paste ONLY the 15-20 lines of code you are troubleshooting here.

[TASK]
Fix the date picker boundary validation logic.

[OUTPUT CONSTRAINT]
Provide ONLY the updated JavaScript function block. Do not write explanations, do not rewrite the surrounding code.