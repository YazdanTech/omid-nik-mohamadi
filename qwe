the html:

<header class="site-header" id="siteHeader">
<div class="header-inner">
    <a href="#top" class="brand">OMID</a>
    <div class="header-actions">
    <button class="theme-toggle" id="themeToggle" aria-label="Toggle light and dark theme">
        <span class="theme-toggle-dot"></span>
    </button>

    <button class="menu-toggle" id="menuToggle" aria-label="Open navigation menu" aria-expanded="false">
        <span class="menu-toggle-line"></span>
        <span class="menu-toggle-line"></span>
    </button>
    </div>
</div>
</header>


the above html's css bellow:


site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 500;
    padding: 1.75rem 0;
    transition: all var(--transition-default);
    border: 1px solid transparent;
}

.site-header.is-scrolled {
    background: var(--nav-bg);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    padding: .8rem 0;
    margin: .9rem 1rem;
    border: 1px solid var(--border-color);
    backdrop-filter: blur(6px);
    border-image: linear-gradient(to right, var(--orange-transparent) 0%, var(--dark-blue) 40%, var(--dark-blue) 60%, var(--orange-transparent) 100%) 1;
    transition: 1s all ease;
    box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.678);
}

.site-header.is-scrolled:hover {
    border-image: linear-gradient(to right, var(--dark-blue) 0%, var(--orange-less-transparent) 40%, var(--orange-less-transparent) 60%, var(--dark-blue) 100%) 1;
    transition: 0.4s all ease;
}

.header-inner {
    max-width: var(--container-width);
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.brand {
    font-family: var(--font-display);
    font-size: 1rem;
    letter-spacing: 0.12em;
    font-weight: 500;
}

.header-actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
}

.theme-toggle {
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    border: 1px solid var(--border-color);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: border-color var(--transition-default);
}

.theme-toggle:hover {
    border-color: var(--accent);
}

.theme-toggle-dot {
    width: 0.6rem;
    height: 0.6rem;
    border-radius: 50%;
    background: var(--accent);
    transition: background var(--transition-default);
}

.menu-toggle {
    width: 2.5rem;
    height: 2.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
}

.menu-toggle-line {
    width: 1.5rem;
    height: 1px;
    background: var(--text-main);
    transition: transform var(--transition-default), opacity var(--transition-default), background var(--transition-default);
}

.menu-toggle.is-active .menu-toggle-line:first-child {
    transform: translateY(0.45rem) rotate(45deg);
}

.menu-toggle.is-active .menu-toggle-line:last-child {
    transform: translateY(-0.45rem) rotate(-45deg);
}



your tasks: 
- menu disppaer, book button appear instead of it at desktop
- website links in the middle of the side-header at desktop 
- for coloring, use text-main, bg-main, accent, accent transparent
- premium vibe animation for hover on links on site header on desktop


output format:
- whenever possible, provide only the code to be placed or updated, not the whole code. provide in seperate code blocks